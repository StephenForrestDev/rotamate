<nav class="navbar navbar-inverse">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <%= link_to "RotaMate", root_path, class: "navbar-brand", id: "logo" %>
    </div>
    <!-- Collect the nav links, forms, and other content for toggling -->
    <ul class="nav navbar-nav navbar-right">
      <% if logged_in? %>
      <li><%= link_to "Drivers", drivers_path %></li>
      <li><%= link_to "Dashboard", dashboard_path(current_driver.id) %></li>
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false"><%= current_driver.name.capitalize %>'s Profile <%= "[Admin]" if current_driver.admin? %><span class="caret"></span></a>
          <ul class="dropdown-menu">
            <li><%= link_to "View your profile", driver_path(current_driver) %></li>
            <li><%= link_to "Edit your profile", edit_driver_path(current_driver) %></li>
            <li role="separator" class="divider"></li>
            <li><%= link_to "Log Out", logout_path, method: :delete %></li>
          </ul>
        </li>
      <% else %>
        <li><%= link_to "Log In", login_path %></li>
      <% end %>
    </ul>
  </div><!-- /.container-fluid -->
</nav>